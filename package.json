{
  "name": "leaderboard",
  "version": "1.0.0",
  "description": "This project creates a leaderboard app that allows the user to add name and score and display the same on the page.",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "webpack-cli": "^5.1.4"
  }
}
